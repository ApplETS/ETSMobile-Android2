<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="LoginFormContainer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">16dp</item>
    </style>

    <!--  Transparent dialog -->
    <style name="CustomProgressDialog" parent="MaterialDrawerTheme.Light.DarkToolbar">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="ETSMobileTheme" parent="MaterialDrawerTheme.Light.DarkToolbar">
        <!-- ...and here we setting appcompat’s color theming attrs -->
        <item name="colorPrimary">@color/ColorPrimary</item>
        <item name="colorPrimaryDark">@color/ColorPrimaryDark</item>
        <item name="colorAccent">@color/ColorAccent</item>
        <item name="android:textColorPrimary">@color/ColorPrimaryText</item>
        <item name="android:textColorSecondary">@color/ColorSecondaryText</item>
        <item name="android:icon">@color/ColorIcons</item>
        <item name="android:divider">@color/ColorDivider</item>
        <item name="android:actionMenuTextColor">@color/black</item>
        <item name="actionMenuTextColor">@color/black</item>
        <item name="colorButtonNormal">@color/ColorPrimary</item>

        <!-- MaterialDrawer specific values -->
        <item name="material_drawer_background">@color/black</item>
        <item name="material_drawer_primary_text">@color/white</item>
        <item name="material_drawer_primary_icon">@color/white</item>
        <item name="material_drawer_secondary_text">@color/white</item>
        <item name="material_drawer_hint_text">@color/black_gray</item>
        <item name="material_drawer_divider">@color/gray</item>
        <item name="material_drawer_selected">@color/black_gray</item>
        <item name="material_drawer_selected_text">@color/white</item>
        <item name="material_drawer_header_selection_text">@color/white</item>
        <item name="material_drawer_header_selection_subtext">@color/white</item>
    </style>

    <style name="ToolbarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/ets_red_fonce</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
    </style>

    <style name="ToolbarPopupTheme">
        <item name="android:textColorPrimary">@color/ColorPrimaryText</item>
        <item name="android:background">@color/transparent</item>
        <item name="colorAccent">@color/ets_red_fonce</item>
        <item name="colorControlNormal">@color/ets_red_fonce</item>
    </style>

    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="WalkthroughTheme" parent="Theme.AppCompat">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
